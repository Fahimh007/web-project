<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-8 text-white">
        <a class="text-blue-400 " href="/">Back to home</a>
        <h1 class="text-3xl mt-6 tracking-tighter mb-3">All Users</h1>
            <div class="users flex gap-3 flex-wrap">
                <% if(users.length){ %>
                    <% users.forEach(function(user){ %>
                        <div class="user w-72 p-4 bg-zinc-700 rounded-lg">
                            <div class="w-full h-72 rounded-lg bg-zinc-800">
                                <img class="w-full h-full object-cover object-top" src="<%= user.image%>" alt="">
                            </div>
                            <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                            <h5 class="text-zinc-400"><%=user.email%></h5>
                            <div class="flex gap-4 mt-8">
                                <a class="text-zinc-200" href="/edit/<%= user._id%>">Edit User</a>
                                <a class="text-red-500" href="/delete/<%= user._id%>">Delete User</a>
                            </div>
                        </div>    
                    <% }) %>   
                <% }else{ %>
                    <h1 class="text-zinc-400">No user yet</h1>
                <% } %>         
            </div>
    </div>
</body>
</html>